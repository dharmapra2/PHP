# Asssignment by internshal

step-1:
    Create database named as <h2>internship</h2>,
    Create table named as <h2>printshastra</h2>,
    aslo create columns as : -`ID`,`name` , `tele` ,`address` , `country` ,`check in date` , `check out date` , `payment_status` , `payment_mode` , `totalcost` .
